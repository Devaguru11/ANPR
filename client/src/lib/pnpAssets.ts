

const base = import.meta.env.BASE_URL;

export const PNP_BADGE_SRC = `${base}PnpBadge.png`;

export const LEGACY_LOGO_SRC = `${base}Logo.png`;

export const ACCESS_GENIE_LOGO_SRC = `${base}AccessGenieLogo.svg`;
